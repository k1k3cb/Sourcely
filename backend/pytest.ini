[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests that hit live external services (Gemini, Groq, Supabase). Skipped unless RUN_INTEGRATION=1 is set.